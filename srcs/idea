Bah quand tu appuis sur tab
T'as une variable static qui se remplis
Un pointer sur la list du readdir, a partir de la tu fais du cmp
Et pour le pointer tu refuse quand t'es a l'interrieur des cote
Et pour le pointeur d'ecriture tu le mouve de la difference etre strlen(lst->nbame) - strlen(sheared)

Touche tab //

Nan c'est jsute le getchar
Tu regarde la'ppel de launch cmd
C'est ici
C'est pas dans launch cmd
C'est dans la lecture de la ligne
